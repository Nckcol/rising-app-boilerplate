import {Component, ViewEncapsulation} from "@angular/core";

@Component({
    selector: "auth",
    styles: [require("./auth.styles.scss")],
    template: require("./auth.template.html"),
    encapsulation: ViewEncapsulation.None
})


export class AuthComponent {
}
